# Core (always required)
numpy==1.26.4
pandas==2.2.2

# Prophet (primary model — installs pystan/cmdstanpy automatically)
prophet==1.1.5

# ARIMA fallback
statsmodels==0.14.2

# Utility
python-dateutil==2.9.0.post0
pytz==2024.1
tqdm==4.66.4
